---
name: Backend-Log
description: Backend log DB, search, decrypt, log type only — LogDb*, SearchSuggest*, Decrypt*, LogType*. Use when task is log/search/decrypt-only.
globs:
  - "backend/src/main/java/**/*LogDb*"
  - "backend/src/main/java/**/*Decrypt*"
  - "backend/src/main/java/**/*SearchSuggest*"
  - "backend/src/main/java/**/*LogType*"
alwaysApply: false
---

# Backend-Log Subagent

Apply when working on **log DB, search, decrypt, or log type** (LogDb*, SearchSuggest*, Decrypt*, LogType*). For auth or activity log/statistics, use Backend-Auth or Backend-ActivityLog.

## Scope (strict)

- **Only**: LogDbController/Service, SearchSuggestController/Service, DecryptController, LogTypeController and related DTOs/services.
- **Not**: Auth, ActivityStatistics, UserActivityLog, ActivityLogAspect, health, or other backend modules.

## Required skills

| Domain | Skill |
|--------|-------|
| Log search / logType / pb_feplog | `log-search-domain` |
| Decrypt approval / snapshot | `search-history-decrypt-domain` |
| Error codes | `error-codes-domain` |

## Role

- Implement or change log search/suggest, decrypt, log-type API only. Follow `docs/contract.md` and specs. Update spec first for these API changes.
- Write or update requirement docs only for log/search/decrypt/log-type requirements.
- Unit/integration tests (JUnit, Mockito). Decrypt/sensitive data per `docs/security-guide.md`.
- When implementing from a requirement doc: after implementation, **confirm or update** that doc's §2 **"Planned change file list"** with the actual files changed; build, restart, then hand off to QA. See `docs/workflow/REQUIREMENTS-AUTHORING-WORKFLOW.md` §1.2.

## References

- Contract: `docs/contract.md`
- Workflow: `docs/workflow/DEVELOPMENT_WORKFLOW.md`
- Prompt (paste into Cursor Settings): `docs/cursor-subagents/backend-log.md`
