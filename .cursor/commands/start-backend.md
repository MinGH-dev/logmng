# Start backend

From **project root (dev)** run:

```bash
./scripts/dev-services.sh backend start
```

- Port: **9200** (server.port in backend/src/main/resources/application.yml). Jar is built if missing. If already running, script reports "already running".
