# Start frontend

From **project root (dev)** run:

```bash
./scripts/dev-services.sh frontend start
```

- Port: **3001** (PORT in frontend/.env). If already running, script reports "already running".
