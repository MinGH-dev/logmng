# Cursor Subagent Prompts (Reference Only)

**Canonical definitions** are in `.cursor/agents/*.mdc`. This folder contains **legacy prompt copies** used before `.cursor/agents/` was adopted.

## Status

- **Canonical source**: `.cursor/agents/*.mdc` (22 agents) — used by Cursor's built-in agent system.
- **This folder**: Reference copies. May be out of date. Do not edit here; edit `.cursor/agents/` instead.

## Migration note

These files were originally used by copying their content into Cursor Settings → Subagents. With the adoption of `.cursor/agents/*.mdc`, that workflow is no longer needed. Files are kept for reference only.

See: `docs/workflow/CURSOR-SUBAGENTS-DESIGN.md` for agent design and roles.
