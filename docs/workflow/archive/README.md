# Archived workflow documents

These documents are internal analysis, review, or superseded files that are **not referenced** by any `.cursor/rules/`, `.cursor/agents/`, or `.cursor/commands/` file.

They are preserved for historical context but moved out of the active `docs/workflow/` path to reduce noise in AI search and exploration.

**Archive criteria**: No reference from any active .cursor rule, agent definition, or command.

**To restore**: Move the file back to `docs/workflow/` and add a reference from the appropriate rule or agent.
