# Canonical activity action_type codes for user_activity_log
# Requirement: docs/requirements/20260330-activity-types-user-mgmt-permission-group.md (OP-01)
# Contract: docs/contract.md, docs/api-definition.md §8
# Step 3 — API/contract only; Backend implements ActivityActionType (or equivalent) + emission points in Step 4.

---
metadata:
  title: "user_activity_log.action_type taxonomy and optional GET /api/activity-log/action-types"
  version: "1.1"
  requirement: "20260330-activity-types-user-mgmt-permission-group, 20260330-permission-group-activity-detail-audit"
  status: "draft"
  op_01_note: >
    Closed set below is the Contract proposal for OP-01. Codes marked **provisional** (or “reserved / mapping TBD”)
    are subject to product confirmation or Step 4 mapping (e.g. SEARCH_HISTORY_* vs SEARCH, approval vs DECRYPT).
    Backend enum and DB VARCHAR(50) must cover the longest code (see OP-04).

---
# 1. Storage and query

- **Column**: `user_activity_log.action_type` — `VARCHAR(50)` (see `schema_user_activity_log.sql`; extend via migration if a future code exceeds 50).
- **Search filter**: `POST /api/activity-log/search` request body **`actionType`** — optional; when set, exact match on `action_type` (existing behavior).
- **Scope**: Same as other activity-log APIs — `activity-log` screen + per-screen scope (`self` | `team` | `all`); see `specs/permission-group-hierarchy.spec.yaml` §4.3.

# 2. OP-01 — Canonical `action_type` codes (UPPER_SNAKE_CASE)

All values are **case-sensitive** strings. Labels (`labelKo`) are **display** hints for UI; authoritative copy may follow product wording.

## 2.1 Implemented today (emit from existing `@ActivityLog` or controllers)

| code | labelKo (hint) | Notes |
|------|----------------|--------|
| `LOGIN` | 로그인 | Auth |
| `LOGOUT` | 로그아웃 | Auth |
| `SEARCH` | 검색 | Log DB search |
| `VIEW` | 상세 조회 | Log detail |
| `DECRYPT` | 복호화 | Decrypt / decrypted view paths |
| `ADVANCED_SEARCH` | 고급 검색 | Advanced search |

## 2.2 Aspect fallback / generic (when `@ActivityLog(actionType="")` infers from method name)

| code | labelKo (hint) | Notes |
|------|----------------|--------|
| `EXPORT` | 내보내기 | Inferred name contains `export` |
| `STATS_VIEW` | 통계 조회 | Inferred name contains `stats`; also counted in search-like KPI bucket in `ActivityStatisticsService` |
| `SCHEMA_VIEW` | 스키마 조회 | Inferred name contains `schema` |
| `UNKNOWN` | 기타 | Fallback when no rule matches |

## 2.3 Planned — permission group (req §2; Step 4 emission)

| code | labelKo (hint) | Notes |
|------|----------------|--------|
| `PERMISSION_GROUP_CREATE` | 권한 그룹 생성 | `action_detail`: ids/codes only; no secrets |
| `PERMISSION_GROUP_UPDATE` | 권한 그룹 수정 | |
| `PERMISSION_GROUP_DELETE` | 권한 그룹 삭제 | |
| `ASSIGN_USER_TO_PERMISSION_GROUP` | 권한 그룹 사용자 배정 | target user + group identifiers |
| `UNASSIGN_USER_FROM_PERMISSION_GROUP` | 권한 그룹 사용자 해제 | target user + group identifiers |

## 2.4 Planned — user & org admin (auditable mutations; Step 4 emission)

| code | labelKo (hint) | Notes |
|------|----------------|--------|
| `USER_CREATE` | 사용자 생성 | **provisional** if product limits scope |
| `USER_UPDATE` | 사용자 수정 | excludes passwords/tokens in `action_detail` |
| `USER_DELETE` | 사용자 삭제 | **provisional** if product has no delete |
| `DEPARTMENT_APPROVER_UPDATE` | 부서 결재자 변경 | department approver screens / APIs |

## 2.5 Reserved / mapping TBD — search history & decrypt approval (OP-01 provisional)

Product may **reuse** existing `SEARCH` / `DECRYPT` for some paths until instrumented. These codes are **reserved** so filters and labels stay stable when split.

| code | labelKo (hint) | Notes |
|------|----------------|--------|
| `SEARCH_HISTORY_CREATE` | 검색 이력 생성 | Use when a distinct row is required vs generic `SEARCH` |
| `DECRYPT_APPROVAL_APPROVE` | 복호화 승인 | Approval action (e.g. search-history approve) |
| `DECRYPT_APPROVAL_REJECT` | 복호화 반려 | Reject action |

## 2.6 Optional — list view audit (OP-03)

| code | labelKo (hint) | Notes |
|------|----------------|--------|
| `USER_LIST_VIEW` | 사용자 목록 조회 | **provisional** — emit only if product accepts noise from `GET /api/users` list |

---

# 3. `action_detail` shapes (high level — no secrets)

JSON object stored in `user_activity_log.action_detail`. Implementations **must not** store passwords, tokens, session identifiers, or decrypted payload content.

| Category | Typical keys (examples) | Forbidden |
|----------|------------------------|-----------|
| **auth** | method, outcome (success/failure without secrets) | password, token |
| **log_search** | `logType`, screen id, row identifiers as already used for audit | full request/response bodies with PII unless policy allows |
| **permission_group** | **`permissionGroupAuditV1`** (see `specs/activity-permission-group-audit.spec.yaml`): `before`/`after` snapshots, nested `allowedScreens` (`AllowedScreenItem`), optional `changeReason` (max 500, provisional). Legacy flat keys may coexist during transition. | raw JWT, encryption keys, denylist keys in §6 of that spec |
| **user_assignment** | `targetUserId` (numeric `app_user.id`), `permissionGroupId` | — |
| **user_admin** | `targetUserId`, optional field names changed (not values for secrets) | password hash, temp password |
| **department_approver** | `departmentCode`, optional approver `userId` references | — |
| **decrypt_approval** | `searchHistoryId`, `decision` (approve/reject) | decrypted content |

Exact key names are implementation details; Contract requires **category-appropriate, non-sensitive** content only.

---

# 4. Optional API — filter dropdown options

## 4.1 Endpoint

| Method | Path | Description |
|--------|------|-------------|
| GET | /api/activity-log/action-types | Returns canonical `{ code, label }[]` for the activity-type filter on **activity-log**. |

**Alternate path (equivalent semantics)** if implementation prefers the shared `filter-options` family: `GET /api/filter-options/activity-action-types`. **Contract canonical path**: **`GET /api/activity-log/action-types`** (first choice in requirement §2).

## 4.2 Auth / access

- **401** if unauthenticated.
- **403** `FORBIDDEN` if the user lacks **activity-log** screen access (`read`), same family as `POST /api/activity-log/search` and `GET /api/filter-options/departments?screen=activity-log`.
- **is_system_admin**: same rules as other activity-log APIs (full access when admin).

## 4.3 Response

**200** — `ApiResponse` wrapper; **data**:

```ts
type ActivityActionTypeOption = {
  code: string;   // e.g. "LOGIN" — matches action_type / actionType filter
  label: string;  // Korean (or product locale) display string for UI
};

type ActivityActionTypesResponse = ActivityActionTypeOption[];
```

- Sort: **`code` ascending** unless product specifies locale-aware sort by `label` (document in release notes if changed).
- Include **all** codes from §2 that the product surfaces in the filter (may exclude `UNKNOWN` from dropdown if product prefers).
- **Empty `actionType`** in search means “all types” (unchanged).

## 4.4 Errors

| Status | code | When |
|--------|------|------|
| 401 | — | Not logged in |
| 403 | FORBIDDEN | No activity-log access |

---

# 5. Statistics bucketing (informative; OP-02)

- **Not a contract for KPI formulas** — product confirms whether new admin types appear in charts.
- Existing SQL maps `LOGIN`, `SEARCH`/`ADVANCED_SEARCH`/`STATS_VIEW`, `DECRYPT` to daily aggregates; new types default to **visible in activity-log list** and **may be excluded** from legacy KPI buckets until product extends aggregation.

---

# 6. Related documents

- `docs/contract.md` — activity-log scope, shared filters
- `docs/api-definition.md` §8 — activity log API
- `specs/permission-group-hierarchy.spec.yaml` §4.3 — screen `activity-log` enforcement
